<?php

    $connection = mysqli_connect(
        'localhost',
        'LaMolecula',
        'Rojo98_leinad0',
        'tasks-app'

    );

   // if($connection) {
    //  echo "Database is connected";
 //   }
